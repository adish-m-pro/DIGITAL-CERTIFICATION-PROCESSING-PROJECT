{
  "name": "digital-academic-certification-verifying-project",
  "version": "1.0.0",
  "description": "Digital Academic Document Workflow System",
  "private": true,
  "scripts": {
    "install:all": "npm.cmd run install:backend && npm.cmd run install:frontend",
    "install:backend": "cd backend && npm.cmd install",
    "install:frontend": "cd frontend && npm.cmd install",
    "dev": "concurrently -n \"backend,frontend\" -c \"blue,green\" \"npm.cmd run dev --prefix backend\" \"npm.cmd run dev --prefix frontend\"",
    "build": "npm.cmd run build --prefix backend && npm.cmd run build --prefix frontend",
    "seed": "npm.cmd run seed --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
